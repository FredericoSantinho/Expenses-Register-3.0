<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_permissions">

  <fragment
      android:id="@+id/navigation_permissions"
      android:name="neuro.expenses.register.ui.permissions.PermissionsFragment">
    <action
        android:id="@+id/action_navigation_permissions_to_navigation_main"
        app:destination="@id/navigation_main" />
  </fragment>

  <activity
      android:id="@+id/navigation_main"
      android:name="neuro.expenses.register.MainActivity" />
</navigation>